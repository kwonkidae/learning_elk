input {
  file {
    path => "/data/chapter04/network_traffic_data.json"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    codec => json {
      charset => "ISO-8859-1"
    }
  }
}
output {
  elasticsearch {
    hosts => "http://host.docker.internal:9200"
    index => "bigginsight"
  }
  stdout {}
}
